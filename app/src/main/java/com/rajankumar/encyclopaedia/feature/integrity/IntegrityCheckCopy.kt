package com.rajankumar.encyclopaedia.feature.integrity

object IntegrityCheckCopy {
  const val heading = "Current data integrity"
  const val description = "Check the study database for invalid IDs, required fields, hierarchy problems, duplicate question-topic links, and broken study or notebook relationships."
  const val runAction = "Run integrity check"
  const val runningAction = "Checking data…"
  const val closeAction = "Close"
}
